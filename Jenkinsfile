@Library('jenkins-shared-lib') _

pipeline {
    agent any
    stages {
        stage('Demo') {
            steps {
                // Call the shared library function
                sayHello('Kushal')
            }
        }
    }
}
